[project]
name = "commonplace-client"
version = "0.1.0"
description = "Example MQTT client for the Commonplace document system"
requires-python = ">=3.10"
dependencies = [
    "paho-mqtt>=2.0.0",
    "y-py>=0.6.0",
]

[project.scripts]
counter = "counter_example:main"
